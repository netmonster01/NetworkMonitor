import { CommentsDirective } from './comments.directive';

describe('CommentsDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentsDirective();
    expect(directive).toBeTruthy();
  });
});
