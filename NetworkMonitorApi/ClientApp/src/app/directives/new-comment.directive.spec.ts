import { NewCommentDirective } from './new-comment.directive';

describe('NewCommentDirective', () => {
  it('should create an instance', () => {
    const directive = new NewCommentDirective();
    expect(directive).toBeTruthy();
  });
});
